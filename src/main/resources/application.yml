spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/homework?serverTimezone=Asia/Seoul
    username: root
    password: cz_hyy6848

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
      use-new-id-generator-mappings: false
    properties:
      hibernate.format_sql: true

  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s

  jackson:
    serialization:
      fail-on-empty-beans: false

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace
